{
  "name": "askk-c2w-vm",
  "type": "module",
  "private": true,
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "xterm-pty": "0.9.4"
  },
  "description": "Builds the container2wasm (64-bit Alpine, Bochs) serial-console bundle (crates/web/assets/vm/c2w.js + c2w-worker.js). Run `bun install && bun run build` after editing entry.js/worker-entry.js, then commit the regenerated assets.",
  "scripts": {
    "build": "bun run build.mjs"
  }
}
