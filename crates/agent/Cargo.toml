[package]
name = "agent"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "Pure step(), AgentState, Effect, the phase machine, the forge (ADR-010)"

[dependencies]
kernel = { path = "../kernel" }
context = { path = "../context" }
module = { path = "../module" }
serde = { workspace = true }

[lints]
workspace = true
