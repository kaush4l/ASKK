[package]
name = "agent"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "Pure step(), AgentState, Effect, the phase machine (ADR-010)"

[dependencies]
kernel = { path = "../kernel" }
context = { path = "../context" }
module = { path = "../module" }
serde = { workspace = true }
# serde_json: tool arguments are JSON, and "unreadable arguments are refused,
# never delivered empty" needs a real parser to know which is which.
serde_json = { workspace = true }

[lints]
workspace = true
