//! Deliberately empty. See `Cargo.toml`: this package exists so that
//! `#[wasm_bindgen_test]` can compile against a crate that does not own
//! `::core`, and its whole content is the files in `tests/`.
