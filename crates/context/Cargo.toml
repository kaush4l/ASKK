[package]
name = "context"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "The Context Document: Section/Document/Part, pure assemble + render (ADR-009)"

[dependencies]
kernel = { path = "../kernel" }
serde = { workspace = true }
# PROVISIONAL (G4): the OpenAI wire body/reply are JSON; hand-rolling them
# would re-implement escaping serde_json already owns. Also the golden files.
serde_json = { workspace = true }

[lints]
workspace = true
