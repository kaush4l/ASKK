[package]
name = "module"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "Module contract, manifest, registry, affordances, view primitives (ADR-004)"

[dependencies]
kernel = { path = "../kernel" }
context = { path = "../context" }
serde = { workspace = true }

[lints]
workspace = true
