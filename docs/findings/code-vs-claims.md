# Code vs claims — where documents lied and the code told the truth

One row per discrepancy; the code always wins (CLAUDE.md §2).

| Date | Claim (source) | Code truth | Action |
|---|---|---|---|
| 2026-07-11 | docs/TESTING.md layer 8: "`wasm-bindgen-test` for OPFS stores + fetch transport" | Zero wasm-bindgen tests exist in any crate (no dep, no test); `crates/web` tests are host-target `#[test]`; gate never even compiled wasm32 | TESTING.md corrected; `cargo check -p askk-web --target wasm32-unknown-unknown` added to gate.sh |
| 2026-07-11 | Brief v4 §4: "a crashed VM **Worker** is a failed effect" | v86 runs on the MAIN thread by explicit decision (scripts/vm/entry.js:11); containment still holds — every VM failure → `ToolResult::err`, run survives | No change; premise noted in brief-v4-gap.md |
| 2026-07-11 | Brief v4 §1 implies the ExecutionBackend seam must be built | `trait ShellExec` (shell.rs:19) already is that seam: injected, 3 impls, nothing above it knows v86 | No rename (ADR-021); brief's concept mapped, not rebuilt |
| 2026-07-11 | Brief v4 §5 A5 "zero duplicate effects" reads as a dedup gap | Dedup is trivially green today only because resume doesn't exist (epoch fence kills non-terminal runs on reopen, log.rs) — the honest gap is RESUME | ROWS.md marks A5 red with foundation pinned by test |
| 2026-07-15 | Prior memory (askk-core-abstract-base-shipped, askk-loop-of-loops-framework) describes an `Engine`/`BaseEngine`/`ReactEngine` trait ladder and a `Strategy`/`DeclaredStrategy` type | No such types exist (`grep -rn` across `crates/` = 0 hits each). The engine is **concrete**: `RunSession` + `Shared` own the session, per-run `RunState` carries its own history/toolset/agent.md/phases (`crates/engine/src/run/session.rs`); the loop is `drive_run` → `one_turn` (`crates/engine/src/run/turn.rs`), a scripted phase runs `run/scripted.rs`. A "declared strategy" is just `AgentConfig.phases: Vec<Phase>` with `RunState.declared = !phases.is_empty()` (`session.rs:181`) — no Strategy object. | NAVIGATION §2 documents the real shape; memory entries are stale, not code |
| 2026-07-15 | Prior memory (askk-fleet-runtime-shipped) describes an `Instance` / `InstanceCollection` / `RunScratchpad` fleet-runtime model | None of those types exist (`grep` = 0 hits). "An engine instance" is a `RunState` keyed by `RunId` in `Shared.runs: RefCell<BTreeMap<RunId, RunState>>` (`session.rs:66`); delegation is `drive_child` running a child run at `depth+1` (`crates/engine/src/run/delegation/delegate.rs`), and the new Fleet stage just launches N top-level runs via `submit` + `drive_run`. | NAVIGATION §2 + the ADR-042 surfaces table reflect the concrete model |
| 2026-07-16 | README "Architecture in six lines": crates layer as `core ← inference ← runtime ← web`; feature list still claims a Kanban work board (ADR-026) and a v86 VM | The DAG is 7 crates — `core ← inference ← state ← features ← engine ← browser ← frontend` (`crates/engine/tests/structure.rs::import_direction_is_one_way`); the board was removed (ADR-040) and v86 replaced by container2wasm (ADR-037) | Logged; README body refresh is its own unit (this unit only added the NAVIGATION pointer) |
