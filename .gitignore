# The JavaScript tree — everything here is produced by a command in package.json.
node_modules/
.next/
apps/web/out/
.deploy/
*.tsbuildinfo
.tsconfig.pkg.json

# OS
.DS_Store

# Claude Code local state
.claude/settings.local.json
.claude/worktrees/

# WHAT THE DELETED TREES LEFT ON THIS DISK. Nothing in the repository refers to
# any of it any more: `target/` and `dist/` were the Rust and Trunk builds,
# `docs/bin/` and `docs/wasm/` were the container2wasm shelf and its guest image
# — a 47 MB Linux emulator this build refuses on the record (docs/RULINGS.md).
# They are listed rather than removed because they are large, they are not in
# any history, and deleting a gigabyte off somebody's disk is theirs to do.
target/
dist/
out/
pkg/
docs/bin/
docs/wasm/
.gstack/
__pycache__/
