{
  "name": "askk",
  "private": true,
  "type": "module",
  "scripts": {
    "agents": "bun scripts/agents.js",
    "dev": "bun scripts/agents.js && next dev --port 3000",
    "build": "bun scripts/agents.js && rm -rf .next out && next build",
    "start": "bunx serve out",
    "lint": "biome check src scripts test bench next.config.js public/sandbox/vm-worker.js",
    "format": "biome check --write src scripts test bench next.config.js public/sandbox/vm-worker.js",
    "test": "bun test --isolate ./test",
    "bench": "bun bench/run.js",
    "bench:blind": "bun bench/blind.js",
    "smoke": "bun run build && bun scripts/smoke.js",
    "check": "bun run lint && bun run test && bun run smoke"
  },
  "dependencies": {
    "@huggingface/transformers": "4.2.0",
    "next": "16.3.3",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.11"
  }
}
