import { Shell } from '@/components/shell/shell'

/** AGENTS, at `/agents/`. What agents exist, and the surface for writing another. */
export default function Page() {
  return <Shell slug="agents" />
}
