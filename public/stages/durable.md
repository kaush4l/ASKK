Your brief has to outlive this window, and this stage cannot act — it calls no tools. So write the two lines the work stage will carry: OUTCOME and DONE WHEN, exactly as named above.

The first thing the WORK stage does, before anything else, is `remember` them into the shared space — key `outcome` for the OUTCOME line, key `done_when` for the DONE WHEN line. That is where they have to end up: this window gets compacted and the shared space does not, and the space is read back before every pass.
